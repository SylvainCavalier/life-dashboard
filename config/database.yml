# PostgreSQL database configuration
# Simple and standard configuration using PostgreSQL environment variables
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 10) %>
  username: <%= ENV.fetch("PGUSER", "postgres") %>
  password: <%= ENV["PGPASSWORD"] %>
  host: <%= ENV.fetch("PGHOST", "localhost") %>

development:
  <<: *default
  database: life_dashboard_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: life_dashboard_test

production:
  <<: *default
  # En prod on passe par DATABASE_URL :  postgres://user:pass@host:5432/dbname
  url: <%= ENV["DATABASE_URL"] %>
